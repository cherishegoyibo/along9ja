import { useState } from "react";

export default function InputField() {
const InputField = ({ type, placeholder, icon }) =>
{
    const [isPaswwordShown, setIsPassword] = useState(false);
}
    return (

    ))
}